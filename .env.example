# AI Error Query Builder - Environment Variables (Cloudflare Workers)

# ============================================================================
# Frontend Configuration (Vite)
# ============================================================================

# API Endpoint (for local development with Wrangler)
# Use http://localhost:8787/api for local Workers development
# Leave empty for production (will use relative /api path)
VITE_API_URL=http://localhost:8787/api

# Feature Flags
VITE_ENABLE_ANALYTICS=false

# Application Settings
VITE_APP_NAME=AI Error Query Builder
VITE_APP_VERSION=2.0.0

# Environment
VITE_ENV=development

# ============================================================================
# Cloudflare Workers Configuration
# ============================================================================
# These are set via Wrangler CLI, not in .env file
# Use: wrangler secret put OPENAI_API_KEY

# OpenAI API Key (required)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# Cloudflare AI Gateway ID (optional, recommended)
# Provides caching, rate limiting, and analytics
# Get from: https://dash.cloudflare.com/?to=/:account/ai/ai-gateway
# AI_GATEWAY_ID=your-gateway-id

# Cloudflare Account ID (required for wrangler)
# Get from: https://dash.cloudflare.com/?to=/:account/workers
# Set in wrangler.toml: account_id = "..."

# D1 Database ID (required for query history)
# Create with: wrangler d1 create ai-query-builder-db
# Set in wrangler.toml: database_id = "..."

# ============================================================================
# Deployment Instructions
# ============================================================================

# 1. Install Wrangler CLI:
#    npm install -g wrangler

# 2. Login to Cloudflare:
#    wrangler login

# 3. Create D1 Database:
#    wrangler d1 create ai-query-builder-db
#    # Copy the database_id to wrangler.toml

# 4. Initialize database schema:
#    wrangler d1 execute ai-query-builder-db --file=./workers/schema.sql

# 5. Set OpenAI API Key:
#    wrangler secret put OPENAI_API_KEY

# 6. (Optional) Set AI Gateway ID:
#    wrangler secret put AI_GATEWAY_ID

# 7. Deploy:
#    npm run deploy

# ============================================================================
# Local Development
# ============================================================================

# Run frontend only:
#   npm run dev

# Run Workers locally:
#   npm run dev:workers

# Run both simultaneously:
#   npm run dev:full
